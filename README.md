# Elementor JSON Lab

Automated validation, dependency scanning and visual preview tooling for Elementor JSON templates.

The implementation lives behind a reviewable branch/PR before it is merged into `main`.
