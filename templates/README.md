# Templates

Place Elementor export JSON files directly in this directory.

Examples:

- `homepage.json`
- `landing-page.json`
- `contact.json`

Each `.json` file is audited and, when compatible with the CI runtime, rendered into a temporary WordPress page for desktop, tablet and mobile screenshots.
